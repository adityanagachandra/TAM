# Core ML and Transformers
transformers==4.52.1
accelerate
torch
torchvision
safetensors

# Computer Vision and Image Processing
opencv-python
pillow

# PDF and Document Processing
pymupdf
# NOTE: Do NOT install 'fitz' package as it conflicts with PyMuPDF

# Scientific Computing
numpy
scipy
matplotlib

# Natural Language Processing
nltk
rouge

# Utilities
pathlib
pyyaml
tqdm
requests

# HuggingFace Hub
huggingface-hub

# Optional: for text visualization (install separately)
# LaTeX installation (platform specific):
# Ubuntu/Debian: sudo apt-get update && sudo apt-get install texlive-xetex
# macOS: brew install --cask mactex
# Windows: Install MiKTeX or TeX Live
